syntax = "proto3";

package platform.service.v1;
import "errors/errors.proto";

option go_package = "github.com/shy/throw_gun/pkg/common_interface/pb_interface/api/platform/service/v1;v1";

enum ErrorReason {
  // 设置缺省错误码
  option (errors.default_code) = 500;

  USER_NOT_FOUND = 0 [(errors.code) = 404];
  CONTENT_MISSING = 1 [(errors.code) = 405];
}
